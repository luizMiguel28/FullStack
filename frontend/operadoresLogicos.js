let n1 = 30;
let n2 = 20;

// let verificador = n1 < n2 && n1 == 30; // AND | &&
// let verificador = n1 < n2 || n2 == 20; // OU | ||
let verificador = !(n1 == 30); // NOT | NÃO | !

console.log(verificador);
